using UnityEngine;

namespace GruntzUnityverse {
  /// <summary>
  /// Editor-only helper script for displaying the range of enemy Gruntz.
  /// </summary>
  public class RangeRect : MonoBehaviour {
    private void Awake() {
      Destroy(gameObject);
    }
  }
}
