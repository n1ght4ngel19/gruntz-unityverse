using System.Collections.Generic;
using UnityEngine;

namespace GruntzUnityverse {
  public class DeathAnimationPack {
    public List<Sprite> DeathBurn;
    public List<Sprite> DeathElectrocute;
    public List<Sprite> DeathExplode;
    public List<Sprite> DeathFall;
    public List<Sprite> DeathFlyUp;
    public List<Sprite> DeathFreeze;
    public List<Sprite> DeathHole;
    public List<Sprite> DeathKaraoke;
    public List<Sprite> DeathMelt;
    public List<Sprite> DeathSink;
    public List<Sprite> DeathSquash;

    public DeathAnimationPack() {
      // DeathBurn = Resources.LoadAll<Sprite>("Animations/Gruntz/Death/DeathBurn").ToList();
      // DeathElectrocute = Resources.LoadAll<Sprite>("Animations/Gruntz/Death/DeathElectrocute").ToList();
      // DeathExplode = Resources.LoadAll<Sprite>("Animations/Gruntz/Death/DeathExplode").ToList();
      // DeathFall = Resources.LoadAll<Sprite>("Animations/Gruntz/Death/DeathFall").ToList();
      // DeathFlyUp = Resources.LoadAll<Sprite>("Animations/Gruntz/Death/DeathFlyUp").ToList();
      // DeathFreeze = Resources.LoadAll<Sprite>("Animations/Gruntz/Death/DeathFreeze").ToList();
      // DeathHole = Resources.LoadAll<Sprite>("Animations/Gruntz/Death/DeathHole").ToList();
      // DeathKaraoke = Resources.LoadAll<Sprite>("Animations/Gruntz/Death/DeathKaraoke").ToList();
      // DeathMelt = Resources.LoadAll<Sprite>("Animations/Gruntz/Death/DeathMelt").ToList();
      // DeathSink = Resources.LoadAll<Sprite>("Animations/Gruntz/Death/DeathSink").ToList();
      // DeathSquash = Resources.LoadAll<Sprite>("Animations/Gruntz/Death/DeathSquash").ToList();
    }
  }
}
